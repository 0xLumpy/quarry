# Quarry v0.3.10 release ledger

**Release theme:** foundational integrity and truthful operation

**Base revision:** `4e4825c6f2a6f2bd81d81da0f231f56845ffd6aa` (`0.3.9`)

**Phase 1 audited source:** `474d848656a01cd484dd62d817eb21d527202a78`
(`d42640ef23b9ae44c2ec09d18ac5a704e4373d05` Git tree), 2026-08-14

**H0 development-diagnostic source:** `19ab50cbdc2415c78e9bb5651dec2e072bb3a71b`
(`8311829d62be4b7099979e2c0e2f476c2f94fc34` Git tree), 2026-08-14

**Decision:** **INTERNAL INTEGRITY COMPLETE**

**Version/tag/package state:** package sources are `0.3.10`; the unsigned
`v0.3.10` tag is a personal bookmark and confirming-CI trigger, not a
distribution claim; no published package exists

This is the sole in-tree release-scope ledger for the unpublished `v0.3.10`
internal-integrity milestone. The package is version `0.3.10`; its tag is a
personal bookmark rather than a distribution event. Scope and result integrity
come from clean commit/tree
identity, canonical content digests, managed tool receipts, sealed evidence,
the actual local/Linux-CI matrix, and the maintainer's unsigned ledger sign-off.
The untracked `notes/V0.3.10-PLAN.md` and handoff statements are
historical planning inputs, not release authority. Finding definitions and
stable identifiers remain in the
[archived register](../archive/audit-v0.3.9/AUDIT_REGISTER_RECONCILED.md). This
ledger holds the current milestone dispositions; the historical
[CURRENT-HEAD.md](../audit/CURRENT-HEAD.md) audit baseline remains an input, not
a current release blocker.

### Dormant publication-quality substrate

`release/evidence/quality-policy-v1.json` is a frozen local non-regression
substrate: it records exactly six pinned formatting, lint, type, documentation,
dead-code and complexity observations and their current ceilings. External
threshold review, attested tool identities, and signed H0 quality reports are
publication apparatus and are `N/A UNTIL PUBLISH` for this milestone.

### Dormant publication static-security substrate

`static-security-policy-v1.json` freezes Bandit HIGH/HIGH `src` scope, its
separate reviewed exceptions, the tracked-file detect-secrets baseline, unsafe
API inventory, named H0 archive/path/config property nodes, and the dependency
manifest. The external candidate collector, signed findings report, and
candidate-bound aggregation remain dormant and `N/A UNTIL PUBLISH`.

The supported interpreter is CPython 3.12 on the maintainer's Linux environment
and GitHub's standard Ubuntu runner. The retained package compatibility range
is `requires-python >=3.10,<3.13`; that metadata is not a claim that untested
interpreters belong to this milestone's support matrix. The support matrix
freezes the exact observed local and CI identities without claiming a signed
runner-image attestation.

### B-DETERMINISM local substrate

`determinism-fixture-v1.json` fixes a small synthetic input for the shared
release-evidence, run-manifest and report serializers. The shard-0 CI producer
creates exactly two temporary roots, retains and reopens every generated file
to hash its bytes, and uploads a candidate-independent paired tree fragment.
The evidence collector later retains that fragment alongside one
candidate-bound `artifact-tree-diff`, binding the exact H0 instance/toolchain,
source inputs, fixture-manifest digest and derived fixture-tree identity. Its
verifier rebuilds every fixture file before accepting `artifact_differences`;
the only accepted limit is zero.

## Release contract

`v0.3.10` is an integrity release, not a feature release. It must establish that Quarry:

1. never reports successful acquisition before primary evidence is durably settled;
2. never mutates a sealed base run;
3. never certifies a revision, manifest or campaign outcome it cannot reconstruct;
4. preserves an honest, bounded and replayable disposition for work it could not retain or execute;
5. launches the artifact and credential set that policy actually approved;
6. permits intended private-target, broad-active and public-OOB behavior while withholding scanner,
   metadata and unrelated control-plane destinations;
7. makes private operator output lossless and does not invent conclusions from absent evidence;
8. ships with reproducible, non-live release gates and basic public-project integrity.

No implementation may close a row by reducing the accepted Nuclei request set, default-denying private
targets, disabling public Interactsh, imposing a small universal time limit, or discarding target evidence.

## User-facing release summary

### Behavioral changes

This release makes execution settlement, repository mutation, revision
publication, campaign truth, runtime identity and network effects explicit and
fail closed. It does not broaden Quarry's target or feature scope.

### Accepted risks

Raw same-UID mutation outside Quarry's authenticated repository operations is
outside the cooperative authority boundary. The larger indexed report/database
redesign and distributed execution remain deferred under the constraints below.

### Known limitations

No public release or distribution claim is being made. Production trust policy,
public signing keys, signature roles/thresholds/rotation, detached approval,
signed tagging, publication receipts, and CI-image attestation are
`not_applicable_until_publish`. The current source is version `0.3.10`.
Resource-performance limits are measured and recorded but do
not block this milestone; integrity outcomes collected by those gates do.

### Migrations

No blanket data-format migration is promised. Unknown or unsupported manifests
fail closed; operators should retain prior runs and use a new output root when
validating the nominated package.

### Exit and result contract

Success is reported only after the primary process, streams, descendants and
durable evidence have settled. Empty, partial, timed-out and refused work retain
typed outcomes rather than being reclassified as successful completion.

### Supported matrix

The milestone matrix is Python 3.12 only, on the maintainer's Linux environment
and GitHub's standard Ubuntu runner. Exact observed interpreter, local/CI environment,
isolation profile, managed tool, template version, and content-digest identities
come from the support matrix. A runner image is an observed environment label,
not an attested VM identity.

## In-scope work packages

Closure follows the internal evidence lifecycle: each applicable obligation has
verified content-bound evidence from the clean candidates and actual supported
environments, followed by the maintainer's unsigned sign-off in this ledger.
Public aggregation, detached approval, signing, and publication do not
participate in this milestone. The personal tag only starts the confirming CI
run. Editing status text alone never creates or changes evidence.

| Work package | Status | QR39 mapping | Required outcome | Dependencies |
|---|---|---|---|---|
| `V310-01` Runner settlement | `CLOSED` (maintainer sign-off 2026-08-22) | `QR39-001`, `004`, `030` | No success/empty result while a primary drain/feed remains live, truncated, unpersisted or unclassified; invalid argv is typed; one deadline covers execution, teardown and drain. | Typed `Fault`/result contract already exists. |
| `V310-02` Repository seal and identity | `CLOSED` (maintainer sign-off 2026-08-22) | `QR39-005`, `006`, `009`, `016`, `032` | One repository mutation boundary validates entity/object/path identity and lifecycle; sealed base evidence is read-only; existing sensitive files are no-follow, owner/mode checked. | Must land before `V310-03`; coordinate with current `privfs`. |
| `V310-03` Revision transaction | `CLOSED` (maintainer sign-off 2026-08-22) | `QR39-009`, `024` | Full prior view plus candidate is recomputed and certified; candidate artifacts/views are durable; pointer publishes last; a failed candidate never replaces a valid pointer. | `V310-02`. |
| `V310-04` Manifest and campaign truth | `CLOSED` (maintainer sign-off 2026-08-22) | `QR39-003`, `012`, `016` | One strict committed-manifest reader validates schema/types/counts/lifecycle; settlement uses it, preserves historical gaps and refuses contradictory terminal ledger state. | `V310-02`; typed state; revision result contract from `V310-03`. |
| `V310-05` Installer/runtime/secret boundary | `CLOSED` (maintainer sign-off 2026-08-22) | `QR39-007`, `008`, `017`, `028`, `042` | Every install strategy verifies before atomic activation and rolls back; runs launch verified absolute payload identities with complete runtime-closure provenance and minimal per-adapter environments. | No dependency on repository refactor; manifest fields coordinate with `V310-04`. |
| `V310-06` Aggregate resource and remainder contract | `CLOSED` (maintainer sign-off 2026-08-22) | `QR39-004`, `005`, `015`, `029`, `030`, `039`; `041` truthfulness subset | Publish and enforce the v0.3.x support envelope; cross-process reservations/leases prevent overspend/collision; refused work is replayable or truthfully terminal; resolver batches have a corpus deadline. | `V310-01` for drain semantics; `V310-02` for durable work records. |
| `V310-07` Connect-time protected destinations | `CLOSED` (maintainer sign-off 2026-08-22) | `QR39-019` | Keep default private reach, but enforce scanner/metadata/control-plane exclusion for hostname, redirect, proxy, direct-IP and CIDR lanes at connection/egress time. | Policy inventory and adapter inventory. |
| `V310-08` Minimum report truth | `CLOSED` (maintainer sign-off 2026-08-22) | `QR39-013`, `022`, `034`, `043` reporting subset | Remove unsupported no-WAF/origin claims, preserve full local evidence/provenance, encode untrusted rendering, distinguish unknown/false, and prevent Nuclei-empty input from skipping independent params lanes. | The two output-truth corrections intentionally ran before further gate collection; closure uses `V310-02` stable references and has no dependency on the future report prototype. |
| `V310-09` Accepted-policy accuracy and provenance | `CLOSED` (maintainer sign-off 2026-08-22) | `QR39-014`, `017`, `027`, `040` | Relabel Nuclei as broad active verification; bind engine/flags/config/template/helper identities; document and test independent OOB disable behavior while retaining public default; preserve private-target opt-out. | `V310-05` identity; `V310-07` network boundary. |
| `V310-10` Release engineering | `CLOSED` (maintainer sign-off 2026-08-22) | `QR39-043` plus release-process gaps | All non-live tests run in CI; verification script is split so offline means no target contact; add committed quality/security/dependency gates and public project/release-integrity files. | All work packages before final candidate evidence. |

`V310-07` evidence is the H1 namespace effect trace together with the
per-invocation policy records and mediator decision output captured by that
gate. This release does not require a second, single-file journal that globally
orders parent-side DNS and worker-side broker events.

## Required implementation order

This order minimizes transitional formats and makes each gate meaningful:

1. Freeze release inputs and sanitized regression oracles; do not alter source engagement runs.
2. Prove the minimum `V310-08` correctness subset in actual rendered output before collecting further
   gate evidence: absence must not become a WAF/origin claim, and private output must not omit a store
   observation or its material/artifact digests.
3. Close `V310-01` and `V310-02`: evidence settlement and repository authority are prerequisites.
   Their shared implementation contract is
   [`PHASE-1-EXECUTION-REPOSITORY.md`](../design/PHASE-1-EXECUTION-REPOSITORY.md).
4. Build `V310-03` and `V310-04` on that boundary; do not add another sidecar format.
5. Close independent `V310-05` while the state work proceeds.
6. Close `V310-06` and publish the honest supported envelope.
7. Close `V310-07` and `V310-09` without changing the accepted defaults.
8. Finish the internal evidence/sign-off projection for `V310-08`; keep the larger report redesign deferred.
9. Complete and freeze `V310-10`, run the applicable gates on clean, quiescent
   candidates across the actual Python 3.12 local/Linux-CI matrix, verify their
   content-bound evidence, record the maintainer sign-off, bump the package to
   `0.3.10`, and use the personal tag run as the confirming gate. No production
   trust policy, detached approval, or publication step is required.

## Phase 1 non-closing implementation record

The audited Phase 1 source repairs the implementation seams that underlie `V310-01` and `V310-02`, but
it is not a release candidate and did not produce the canonical evidence required to change either row.
The documentation commit carrying this record is not treated as a self-audited source revision.

| Scope | Narrow implementation evidence | Release disposition |
|---|---|---|
| Runner settlement foundation | Supervised worker ownership, typed authenticated settlement, repository-composed stdout/stderr publication and current-attempt output semantics; 38/38 runner policy sites carry the triad (37 `exec_tool` facade sites and one internal repository delegation), and 15/15 production `run_contract` callers supply it. `C-OUTPUT-CONTRACT` freezes and executes all nine cases through the repository runner, emits typed internal receipts, and verifies their digest-linked matrix. Managed Gitleaks v8.30.1 supplies the two native cases; each produced one committed current report and one parsed finding. `C-FAULT-RUNNER` freezes an eleven-boundary, 58-node mixed H0/H1 source plan. | On clean candidate `e35e045`, `C-OUTPUT-CONTRACT` passed 9/9 and its independent verifier accepted matrix digest `sha256:52cc693dbd74c54637998a11983668426c9d155e35c789961ab1bac360c2cdab`; `C-FAULT-RUNNER` passed its exact 58-node roster (32 H0 and 26 H1). `V310-01` remains `OPEN` only for candidate-wide `B-HERMETIC-ALL` and the approved integrity portion of `C-PERF-RUNNER`. |
| Repository and managed-acquisition foundation | Production Run-base writers share mutation/artifact authority; the base seal is irreversible; budget-ledger persistence, exact canonical removal, and managed HTTP body/receipt publication, replay and conditional discard use that authority. The final classifier excluded 39 value transforms from 284 terminal-name candidates, semantically reviewed the remaining 245 concrete writer/mutation candidates and found zero ambient canonical Run-base writers; 21/21 native sinks were authorized. `C-PRIVATE-FILES` binds separate H0/H1 measurements and requires an executed foreign-owner refusal. A fixed 96-case `C-PATH-IDENTITY` corpus and measured containment-decision producer are scope-bound. `C-FAULT-STORE` freezes a nine-boundary, 60-node source plan and reconciles it against the executed H0 roster. | On clean candidate `e35e045`, `B-MANIFEST` passed 40/40, `C-PRIVATE-FILES` observed the real foreign-owner refusal, `C-PATH-IDENTITY` reconciled all 96 cases, and `C-FAULT-STORE` passed 60/60. The content-bound internal evidence is complete; the maintainer signed off `V310-02` on 2026-08-22 after the four named suites passed 170/170 at the exact candidate. |
| Focused diagnostics | Managed-acquisition integration: 562 transaction/legacy passes, independent 8-case replay matrix, 119 transaction passes on Python 3.13, 11 final static/currentness passes on Python 3.13.12, and 71 independent authority/currentness passes. At the exact audited source, fresh clean-archive matrices collected 7,631 tests per interpreter: default passed 7,528, offline passed 6,224, and integration passed 103 on each of Python 3.10, 3.12 and 3.13. The final writer audit passed 32 focused plus 2 static tests; `474d848` test hygiene passed its affected Python 3.13 sequence 50/50. Exact hashes, deselections, warnings and timings are in [CURRENT-HEAD.md](../audit/CURRENT-HEAD.md#evidence-baseline). | Diagnostic only; no `RG00`–`RG09` evidence slot is populated. |

## Clean-candidate V310-01/V310-02 checkpoint

The following unsigned internal evidence was collected on 2026-08-21 from a
clean, quiescent Linux tree. It is bound to commit
`e35e0455d6fe3fa4304530341d2da6f8f3178b55`, Git tree
`18f81a65cdcda1dfb7b5dee9bd910ddd5ebdd6aa`, source-tree digest
`sha256:aca701d29ed852c2095063fd14eed73bc3b5de0220604d637d37ac2f84d47236`,
and candidate-identity digest
`sha256:d49261c377d29fbaf553770ffc49a704792e0450df6787d33dba00ebec5616e4`.
The package version remained `0.3.9`; the Quarry tests and collectors ran under
CPython 3.12.13. No distribution signature or VM-image attestation is claimed.

| Gate | Actual result | Retained artifact SHA-256 |
|---|---|---|
| `C-OUTPUT-CONTRACT` | 9/9 cases passed: empty, non-empty, malformed, truncated, non-UTF-8, partial, timeout, signal, and tool-specific exit. The separately invoked verifier accepted all receipt and candidate bindings. | Matrix file `8516f59803bc930019a17731b6c71d2183b2df549fec129fb89073b0800018a9`; canonical matrix digest `sha256:52cc693dbd74c54637998a11983668426c9d155e35c789961ab1bac360c2cdab`. |
| `C-FAULT-RUNNER` | Exact frozen roster passed 58/58: H0 32/32 and H1 26/26. | H0 JUnit `87625112b8f1781703c649f09c2f9e42a5ffb53230756fe6c160bf08f662524c`; H1 JUnit `dff5684739c1f250e71d97d46ab563fbad7ca18986b83a09ce791c19ef31f3fe`. |
| `B-MANIFEST` | Exact frozen roster passed 40/40. | JUnit `1855a41cc7f4d77e0b098990fe8cdbb35a99dfeefd8ce991a1ef2b819dd5d65d`. |
| `C-PRIVATE-FILES` | H0 creation/mode/symlink checks passed. Privileged H1 actually changed ownership to UID 65534 and observed fail-closed `PrivatePathUnsafe` refusal with errno 40; the pre/post owner remained UID 65534. | H0 filesystem trace `4ef1f2bf86f7ddd35a2a35cd273a606539f692c166d6997a274e95fba56bb7fb`; H1 mode matrix `4f2d2bc19373fd2ec21ea6381cb05e666a7afe4ab73cdd65f50e7ca7c60c0c80`. |
| `C-PATH-IDENTITY` | All 96 frozen cases reconciled: 19 accepted and 77 refused, with zero decision mismatches. | Decisions `d7c751b615aa164544a3c406969a6651f03cb40ec7a790d283714ce9f4ed3597`; corpus `a31dec8bfc27507a51b4b9133e55cfc5d0cc895b492e672e226f68aac0c7ea32`. |
| `C-FAULT-STORE` | Exact frozen roster passed 60/60. | JUnit `acfaaf74b6c4eb92c7a4d7d8743cbab34c208a87eba5eb924ff9d2fd36e3cec6`. |

This checkpoint alone did not close `V310-01`; the later clean local/Linux-CI
result and the integrity-class part of `C-PERF-RUNNER` completed that evidence.
On 2026-08-22 the maintainer approved the benchmark split: hangs,
resource overspend, evidence loss/corruption/omission, leaked execution state,
and silent starvation remain blocking integrity failures; latency, throughput,
RSS, disk-use, and artifact-size measurements that do not violate an integrity
invariant are record-only. No hard performance limit is introduced for
v0.3.10.

Before sign-off, the exact candidate was rechecked on CPython 3.12.13:
finalization-seal 10/10, mutation-authority 110/110, path-identity 22/22,
and private-files 28/28, for 170/170 passes with zero failures, errors, or
skips. The JUnit artifact SHA-256 is
`b3b79d43786b0ddae6beb2e39d5d4b539d21423e07dba8206d079d51c253d6a2`.

`V310-02 maintainer sign-off: APPROVED — 0xLumpy, 2026-08-22, candidate sha256:d49261c377d29fbaf553770ffc49a704792e0450df6787d33dba00ebec5616e4`

The cooperative Run authority serializes participating Quarry writers and reauthenticates exact objects.
Arbitrary raw same-UID mutation after the last authenticated check remains outside Phase 1, consistent
with the `privfs` trust boundary. The clean-candidate checkpoint above supersedes the earlier diagnostic
absence statements for `V310-01` and `V310-02`; it does not manufacture the remaining runner gates or
the maintainer's sign-off.

## V310-08 actual-output correctness checkpoint

The minimum product-truth subset ran first, before additional gate collection,
against the committed synthetic fixture
`tests/fixtures/v310-report-synthetic-corpus-v1.json` (raw SHA-256
`a63fc7a02b73888fc224276fb3cf2424972ca185fc64cee533118c819ca61814`).
The real rendering path created run `20260821-120000-00000008` and committed
manifest digest
`sha256:1b269b1d3e83ba3b4df7f5b496d818d44d253603850e1511afa4f608a96e3286`.

The rendered `digest.json` has raw SHA-256
`e4f3ac0499139a863a01cce19ef636c59c335622f45fdde88647f393abe6083c`.
Its detector-negative row says "direct-service candidate only (WAF state not
inferred from the CDN detector)"; both unknown and absent rows say "CDN
classification unknown; no direct-origin or WAF inference"; the only origin-IP
row says it came from "affirmative synthetic correlation". Inspection found
zero unsupported no-WAF, no-origin, or direct-origin conclusions.

The rendered `private-report.json` has raw SHA-256
`4d810c0072537f0def9c2b2241209967a0faf603be684d6618176e96fea92a3e`.
It reports `input: 9`, `included: 9`, and `omitted: 0`. Store and emitted
counts match exactly by entity: `live` 4/4, and `review`, `secret`,
`subdomain`, `tech`, and `url` 1/1 each. Every emitted row carries its
recomputed `material_digest` and at least one content-digested artifact
reference; the renderer would raise instead of publishing a count gap.

The focused implementation suite passed 134/134 on CPython 3.12.13, covering
unknown/false semantics, inert Markdown rendering, observation and artifact
provenance, fail-closed count/digest reconciliation, and Nuclei-empty lane
independence. This checkpoint accepts the two product corrections in actual
output. `C-CORPUS-SYNTHETIC` now freezes that exact fixture digest and the
unsigned disclosure-attestation digest
`sha256:eeb20c0f8f6a450a61188eb37c319218343a752db0396ee0ba7dd377e72384bc`.
The attestation binds two matching isolated canonical derivations, schema
digest `sha256:824402af1bfff632dabb23be0ac1202db570278ceef75b300f9dc0132a9ea29b`,
and synthetic-value inventory digest
`sha256:a002c4c249722fc3aa985a4e70aba8d93b125d4e389b4492b46f344fbb309f91`.
Its publication-selection approval remains dormant and `null`; the
maintainer's ledger sign-off accepts the corpus for this internal milestone.
The record-only `C-PERF-REPORT` measurement then ran at commit
`9f43121` (candidate identity
`sha256:15f29f9b3339b5121fba0d923b353fd81cd1a4bb1048b82a34e7d0de328c44af`)
against the existing deterministic 24,068-row synthetic-rich projection. All
three raw trials included 24,068/24,068 observations with zero omissions and
produced the same private-report digest
`sha256:a881f948da916261e8d5863104253fa588f963c816afc45c433d45b4e68655bf`.
The measured maximum artifact size was 16,176,288 bytes, p95 wall time was
4,330 milliseconds, and p95 peak process RSS was 206,577,664 bytes. The
canonical descriptive report SHA-256 is
`64f2f278f79b9dd69f316fe325a375e03110ae70eecbc89c305f4b0c24853f86`.
These time, RSS, and size values remain record-only; zero omission and zero
artifact difference are the blocking integrity outcomes, and both passed.

## Clean local candidate checkpoint

This is a documentation-only projection of immutable result identities; the
commit carrying this paragraph is not the tested candidate. The clean,
quiescent local matrix ran against commit
`e55dd3d2024a9a429fe61e3d3e74e4b376ec3426`, Git tree
`14276d5023d400cbc793899d03d50e18ed4221c2`, source-tree digest
`sha256:b9cc58e17ad5768e94a3b9a27df0f78bb727728acec3c76bbe0db162ca6e3e32`,
and candidate identity
`sha256:8a179d89f722c20312500587e1470a3157c764a5c9278c867c9c827b0d793f34`.
The package remained `0.3.9`.

The six local CPython 3.10 H0 shards selected the complete 9,206-node roster:
9,204 passed, zero failed/xfailed/xpassed, and two `pidfd_open` capability
cases skipped because the local interpreter lacks that API. All six shard exit
codes were zero. The full-roster digest is
`sha256:98db4d877117ed710ae23b75abfaf00a6fcd359884fad2dec664c71e5f293e20`.
The ordered shard-report raw SHA-256 values are `fc0ba3b4f05f276be6f0468acd9ea74ac60c70a006626e60bb0cdbc07b8735c2`,
`9c406a93400b7a34716dbbe7240f682bfacd08a6eb318e12dd06c59a3cb8b89b`,
`93a535e10f121d5841f5d406f7ebf5cb061ca02b54f1c15bcc1724e900bc4204`,
`2d40d684d73769c937b790ea5d9d26c23e0f6d6e9a913310e65716185a1f7ff7`,
`b7228f1a62c5833f517de638ac74e66fc62407360aecf85525aea0159be4d490`,
and `3dc9f4d0c9e58c2922f9580000d675780b3680b18d44239862d18ff75b999819`.
Those two nodes were later capability-gated with an explicit reason on runners
without `pidfd_open`; run #63 confirms the supported Linux environment is
green. CPython 3.12 H1 passed 91/91 with no skips; its JUnit
SHA-256 is `5a35093a6578a9d6055a7cdbe11efddb2ded14cc1203c29768a1c8951a09a513`.

The local CPython 3.12 P0 check built the candidate twice from detached clean
worktrees. Wheel bytes matched exactly at
`sha256:fda11a5ada7b99e57e20e0e67dcaae68a349e22c349eedc181aacbb036de16b5`;
the sdist member bytes matched with only archive mtimes excluded by the
committed comparator. Twine metadata, isolated wheel install, CLI/resources,
and the four packaging assertions passed. The installed-runtime SBOM
observation SHA-256 is
`9a951de3b91bbcc63ad6020cc88d5a36745f3db39a224d1664f42389a5d58c29`;
the retained CycloneDX dependency-audit output SHA-256 is
`858c73851dddbdea82455bc933b9af3acd1c1cca1e21b6d0fbf83f5a2fee14e7`
and reported no known vulnerabilities. Fatal Ruff checks and the four-module
Mypy boundary both passed. This historical multi-interpreter diagnostic is
superseded for milestone support by Python 3.12-only local and GitHub CI.

## Final pre-tag closure checkpoint

GitHub Actions [run #63](https://github.com/0xLumpy/quarry/actions/runs/32574908000)
ran by manual dispatch against pre-tag candidate
`a460633bc27f4a248d837539e38f36e5f2b976d6` on GitHub's standard Ubuntu
runner. It completed successfully on 2026-08-22: package passed in 56 seconds,
integration passed in 1 minute 3 seconds, and the full offline job passed in
44 minutes 46 seconds. All three jobs used Python 3.12, and every reported step
passed.

The quiesced local Python 3.12 offline suite selected 9,206 tests: 9,204 passed,
two `pidfd_open` capability cases skipped with an explicit recorded reason, 95
live tests were deselected, and zero tests failed. The run emitted four warnings
and completed in 2,140.39 seconds. Together with the retained focused and
package evidence above, run #63 closes the ten former CI-pending package rows.
The tag-triggered run on the final version/docs/evidence commit is the
confirming gate; it does not create distribution provenance.

## Record-only resource measurement checkpoint

The four remaining descriptive resource workloads ran against exact candidate
`e55dd3d2024a9a429fe61e3d3e74e4b376ec3426` (candidate identity
`sha256:8a179d89f722c20312500587e1470a3157c764a5c9278c867c9c827b0d793f34`)
on local WSL2 Linux, CPython 3.12.13, with 22 logical CPUs. Each workload used
three retained repetitions, no warmup, and 5 ms aggregate process-tree
sampling. The canonical descriptive report SHA-256 is
`3429b80a369df8829a759a314c5246d50fb3b4b0f7227c37a81e5d54fc8036fd`.

| Gate | Actual workload and integrity result | Record-only observations |
|---|---|---|
| `C-PERF-INGEST` | Eighteen trials covered 100/1,000/5,000-row typed ingestion plus 5,000-row merge, reopen and export. Every operation emitted all accepted entities and recorded zero refused remainders. | Per-case p95 wall time was 248/2,660/13,546 ms for small/medium/large, 12,219 ms merge, 108 ms reopen and 103 ms export. Highest per-case p95 aggregate RSS was 38,121,472 bytes; maximum disk growth was 830,000 bytes and maximum measured write amplification was 29,507 basis points. Fixture digest `sha256:8b0374180fadd6d0e28fa4341c3ed711a458db233067ef95f1f0f27e6351d2fb`. |
| `C-PERF-CAMPAIGN` | Each trial exercised the actual eight-child settle/union/decision path with 25 new records per child. All eight children manifested, the final valid union contained exactly 200 records, and the named stop was `max_runs`. | P95 post-child decision latency was 1,477 ms, p95 aggregate RSS 31,076,352 bytes, and p95 disk 442,519 bytes. Seven additional whole-corpus generation snapshots were retained; this is the measured v0.3 immutable-generation cost, not evidence loss or mutation. Fixture digest `sha256:4339d720f17dd0ae747f5d88c0d4af5a9ffee5fe6626b0c9bf7507bacf002483`. |
| `C-PERF-DISK` | Eight concurrent governors each wrote 1 MiB through the real reservation/publication path. All 24 writers completed across three trials: reserve overshoot 0, destination corruptions 0, lost reservations 0, fairness 10,000 basis points. | P95 wall time was 448 ms; observed throughput was 18,724,571–23,301,688 bytes/s and peak aggregate process-tree RSS was 238,301,184 bytes. Fixture digest `sha256:0887cd552faf1d4749d7940b6aa161c7adb781f9d55fa375cae624b3639f802b`. |
| `C-PERF-RESOLVER` | Each trial sent 256 fast plus 256 hung names through the actual bounded worker path. Worker and outstanding-queue peaks were both 16; every unresolved host appeared in the durable remainder, with zero lost remainders, late mutations or surviving workers. | P95 wall time was 777 ms. The 750 ms scheduling deadline settled, reaped workers and persisted the remainder in at most 759 ms; peak aggregate process-tree RSS was 421,756,928 bytes. Fixture digest `sha256:68dd888e06667c5d30928222838af7b705c35f17ca0ecacfef38014b752a0ffc`. |

All 27 retained trials passed their integrity assertions. The latency, RSS,
disk, amplification, throughput and snapshot-count values remain record-only;
the threshold manifest still contains no hard performance limits. No
right-sizing decision here weakens observation provenance, sealed-run
immutability or reproducibility.

## H0 development-diagnostic implementation record

The current source contains a bounded Linux collect-only runner for the exact H0 taxonomy. It captures an
exact commit, creates a private clone and `.git`-free candidate export, binds the committed development
profile, taxonomy/job-map schemas, job map and workflow, and launches isolated Python behind a blank-root
bubblewrap boundary. It publishes private create-only artifacts and deliberately finishes with a
non-authoritative summary rather than a release-gate record.

One run against the exact source above bound source-tree digest
`sha256:53e5c9d77e9e234873b44cabbb219652abdf69dc6a0b2b12977d37a108ba8209`. CPython 3.13.12 and pytest
9.0.3 collected 7,867 nodes, selecting 7,796 H0 and deselecting 71 H1; the taxonomy artifact has raw-file
SHA-256 `732f26bfb49c48ad2bce556d782d43b3952b9dd1b661e5312a30705994c0938b`. Every reported inner
isolation check was true.

This result cannot close a gate. The runner mounts an untrusted development-host `/usr` executable
closure, does not attest a complete runtime image, has no seccomp profile, and executes no tests. The
summary has raw-file SHA-256 `9e52b4761325961ca7bbed8d60f2d1c4163163100cee3be61ec06b5c887ad97f`,
declares `authority: none` and `promotion_eligible: false`, and records `A-TAXONOMY` as `open` for
`non-nominated 0.3.9/development host runtime`. It occupies no v0.3.10 evidence slot. Those historical
`OPEN` results do not override the internal milestone closure recorded here.

## Explicitly deferred scope

Deferral means the limitation is documented and retained in the backlog; it does not mean completed.

| Deferred item | Target | Constraint before deferral is acceptable |
|---|---|---|
| Full disk-backed indexed observation/relation repository and removal of whole-corpus materialization (`QR39-041`) | `v0.4` | `v0.3.10` publishes an enforced support envelope and never describes key-only loss as resumable. High-scale claims remain qualified until this lands. |
| Complete relationship model, temporal edges, query API and report redesign | `v0.3.11` design, `v0.4` implementation | `V310-08` removes false claims and evidence loss from current private reports first. |
| The existing reporting/database prototype from the disposable audit workspace | after observation/relation contracts stabilize | Preserve it as a design and oracle input. Do not copy its heuristic redaction into private reports and do not make its database canonical. |
| AI assessment sidecar and collaborative/tenant features | `v0.5+` | Canonical evidence stays immutable; AI is initially read-only over a typed policy view and appends cited assessments with model/prompt/input-view identities. Tenant/authz/retention boundaries precede sharing. |
| Distributed workers and external plugin SDK | after the single-host repository/executor conformance suite | No global state, unfenced lease or path-based authorization may cross the service boundary. |
| Phase-monolith decomposition | `v0.4` | Split only after `RunContext`, repository, executor and source-adapter contracts exist. |
| Remaining non-blocking QR39 tail | `v0.3.11`/`v0.4` according to the current-head ledger | Each item remains explicitly tracked; no release note may imply blanket audit closure. |

## Dormant publication evidence projection

The external-release definitions, schemas, aggregate, signature verifier, and
publication lifecycle remain in [`RELEASE-GATES.md`](RELEASE-GATES.md) for a
future real distribution threat. For this unpublished, sole-maintainer
milestone, `RG00` through `RG09`, `E-AGGREGATE`, `E-APPROVAL`, signed findings,
signed tags, and publication receipts are all **`N/A UNTIL PUBLISH`**. Their
dormant `OPEN` fixture values are not ship blockers and do not override the
internal package closures in this ledger.

Quarry-native evidence integrity remains applicable: immutable candidate and
artifact digests, managed tool receipts, repository sealing, lossless output,
reproducible fixtures, fault assertions, and the actual Python 3.12 local/CI
results. `C-TOOLS` passed 26/26 capability and identity checks (24
Quarry-managed receipts plus host digests for distro-owned `whois` and `dig`).
`C-OUTPUT-CONTRACT` passed all nine repository-runner cases, and its independent
verifier accepted the receipt bindings and canonical matrix digest.

The threshold manifest and its approval/signature path are also dormant and
retain null `C-PERF-*` hard limits. The approved internal benchmark rule is authoritative:
hangs, resource overspend, evidence loss, corruption or omission, leaked
execution state, and silent starvation are blocking integrity failures;
latency, throughput, RSS, disk use, and artifact size are performance
measurements recorded without blocking `v0.3.10`.

## Per-package closure record

This table is a documentation projection, not the canonical result store. For
the unpublished internal milestone, fill it only from verified candidate-bound
evidence after the corresponding gates run, and cite the exact candidate and
artifact digests. The maintainer's unsigned ledger sign-off closes a package;
the dormant external-distribution aggregate and detached approval do not. Do
not paste credentials, target payload values or sensitive engagement paths
here.

| Work package | Implementing commit(s) | Acceptance gate(s) | Evidence artifact + SHA-256 | Reviewer | Closure status |
|---|---|---|---|---|---|
| `V310-01` | Runner/output implementation through `75c0e29`; evaluated at `e55dd3d` and run #63 | `B-HERMETIC-ALL`, `C-OUTPUT-CONTRACT`, `C-FAULT-RUNNER`, `C-PERF-RUNNER` | Checkpoints above; local H0 9,204 pass/2 explicit capability skips, H1 91/91, `C-OUTPUT-CONTRACT` 9/9, `C-FAULT-RUNNER` 58/58; GitHub run #63 green | 0xLumpy, 2026-08-22 | `CLOSED` — evidence verified and maintainer signed off |
| `V310-02` | Repository/manifest/evidence implementation through `e35e045` | `B-MANIFEST`, `C-PRIVATE-FILES`, `C-PATH-IDENTITY`, `C-FAULT-STORE` | Clean-candidate checkpoint above; candidate `sha256:d49261c377d29fbaf553770ffc49a704792e0450df6787d33dba00ebec5616e4`; four-suite JUnit `b3b79d43786b0ddae6beb2e39d5d4b539d21423e07dba8206d079d51c253d6a2` | 0xLumpy, 2026-08-22 | `CLOSED` — evidence verified and maintainer signed off |
| `V310-03` | Revision transaction through `66e4df2`; evaluated at `e55dd3d` and run #63 | `B-MANIFEST`, `C-FAULT-REVISION` | Focused 42/42 JUnit `45558d62bdfa7f7674a5d3d3cffba45104652a83b48296ee1c1a4d5271d07c59`; included in complete local and GitHub rosters | 0xLumpy, 2026-08-22 | `CLOSED` — evidence verified and maintainer signed off |
| `V310-04` | Manifest/campaign truth through `3f1efb1`; evaluated at `e55dd3d` and run #63 | `B-MANIFEST`, `C-FAULT-FINALIZE`, `C-FAULT-CAMPAIGN` | Focused 75/75 JUnit `8ab5b114b0ee7ef3f7c3bc997789208f3987077eab171d69be4a246f9bd0e686`; included in complete local and GitHub rosters | 0xLumpy, 2026-08-22 | `CLOSED` — evidence verified and maintainer signed off |
| `V310-05` | Installer/runtime/secret boundary through `e35e045`; evaluated at `e55dd3d` and run #63 | `C-INSTALL-ROLLBACK`, `C-SECRETS`, `C-EXEC-IDENTITY`, `C-FAULT-INSTALL` | Focused 87/87 JUnit `8f20190da23eb2b5ee77139b1db692c88c2e4c96c2d45f2e6fe30d175d2e19f5`; local H0/H1/P0 and GitHub run #63 green | 0xLumpy, 2026-08-22 | `CLOSED` — evidence verified and maintainer signed off |
| `V310-06` | Resource contract `56e04cd`; published envelope `9f43121`; evaluated at `e55dd3d` and run #63 | `C-FAULT-DISK`, `C-FAULT-RESOLVER`, applicable integrity outcomes of `C-PERF-*` gates | Focused 56/56 JUnit `be0e347e2ae5dfc2501a0ee8320a8e52636e91f9358bf8155e9d620596fa46df`; complete H0 roster; 27/27 record-only trials, report SHA-256 `3429b80a369df8829a759a314c5246d50fb3b4b0f7227c37a81e5d54fc8036fd`; run #63 green | 0xLumpy, 2026-08-22 | `CLOSED` — integrity assertions passed; performance recorded; maintainer signed off |
| `V310-07` | Network boundary through `8d92695`; evaluated at `e55dd3d` and run #63 | `C-NETWORK-BOUNDARY`, `C-NET-DENY` | Focused local 208 pass/2 explicit pidfd capability skips JUnit `8e88254d7eacf3add5033099b1d6ab4346cd58e34ae5323806c7346ee0d12dbd`; CPython 3.12 H1 namespace adversary and GitHub run #63 passed | 0xLumpy, 2026-08-22 | `CLOSED` — evidence verified and maintainer signed off |
| `V310-08` | `3f1efb1`, `d35d994`; evaluated through run #63 | `B-DOCS-POLICY`, `C-CORPUS-SYNTHETIC`, integrity outcomes of `C-PERF-REPORT` | Actual-output checkpoint above; fixture `a63fc7a02b73888fc224276fb3cf2424972ca185fc64cee533118c819ca61814`, disclosure attestation `eeb20c0f8f6a450a61188eb37c319218343a752db0396ee0ba7dd377e72384bc`, private report `4d810c0072537f0def9c2b2241209967a0faf603be684d6618176e96fea92a3e`; run #63 green | 0xLumpy, 2026-08-22 | `CLOSED` — actual output and integrity evidence verified; maintainer signed off |
| `V310-09` | Accepted Nuclei/policy behavior through `0212e31`; evaluated at `e55dd3d` and run #63 | `B-DOCS-POLICY`, `C-POLICY-TRACE`, `C-EXEC-IDENTITY` | Focused 83/83 JUnit `a14115032b0543de95501ea0f1c5515bfb424e17aac79691a58b34c15559df27`; included in local/GitHub tests | 0xLumpy, 2026-08-22 | `CLOSED` — evidence verified and maintainer signed off |
| `V310-10` | Release engineering through `a460633`; confirmed by run #63 | Internal docs, hygiene, package, integration, and offline gates; distribution ceremony N/A-until-publish | GitHub run #63: package, integration, and offline jobs all green on Python 3.12 | 0xLumpy, 2026-08-22 | `CLOSED` — actual GitHub runner green and maintainer signed off |

## Internal milestone completion rule

The tree is package version `0.3.10`. Completion requires the scoped
integrity implementations, frozen inputs/corpus identities, managed tool and
output receipts, applicable fault checks, repository seal, actual support
matrix, and one clean local/Linux-CI result set. Resource benchmark values are
recorded for review without becoming blockers. The maintainer's ledger sign-off
closes the internal milestone.

`V310-01` and `V310-03` through `V310-10` maintainer sign-off: **APPROVED —
0xLumpy, 2026-08-22**, based on the package-specific evidence above and green
GitHub Actions run #63. `V310-02` retains its earlier exact-candidate sign-off.
All ten packages are closed, completing the internal-integrity milestone.

Production signing policy, detached approval, signed tags, and publication
receipts are deferred until Quarry is published, gains a second maintainer, or
is operated as a service. Dormant schemas and verification code are retained,
but are not wired into `v0.3.10` completion.

The following publication procedure is retained for that future trigger and is
not applicable to the current milestone.

For a future published release, accepted distribution evidence is collected
only after its dedicated nomination commit, against that quiescent candidate
identity. Before tagging or publishing, all of the following must be true:

- every in-scope work package, including the pre-publication acceptance scope
  of `V310-10`, is closed by the accepted aggregate;
- every scope-selected aggregate-input gate record is `pass` or validly
  `not_applicable` for that exact candidate;
- all deferred limitations are accurately represented in the accepted
  release-note artifact;
- no unresolved stop-ship row remains in the candidate-bound current-head
  audit input;
- candidate-built package and provenance subjects have verified accepted
  digests; and
- the maintainer's detached approval binds the aggregate digest.

The signed publication tag must target its nominated candidate commit. Publication
must promote the exact accepted artifact bytes and retain a digest-bound receipt;
it must not rebuild from a later documentation or version commit. Any tracked
change outside designated non-authoritative projection/index fields after
nomination creates a new candidate and repeats every affected gate.

The exact signed tag and publication receipt then satisfy the remaining `RG09`
postconditions and may be mirrored in the post-publication `V310-10`
documentation projection. They are not inputs to the aggregate, approval or
pre-tag `V310-10` acceptance and therefore cannot block their own creation.

After acceptance, a descendant commit may reconcile this ledger and related
indexes as a documentation-only projection. It must cite the candidate commit,
tree and identity digest; the aggregate and approval digests; the signed-tag
object identity; and the publication-receipt digest. It must not alter the
candidate's frozen scope and is never the release subject or evidence authority.
This future procedure does not apply retroactively to the personal `v0.3.10`
bookmark. The internal milestone is complete at package version `0.3.10`; its
tag-triggered CI run confirms the final closure tree.
